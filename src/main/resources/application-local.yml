spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tax
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # 필요 시 validate, create 등으로 변경
    show-sql: true
    properties:
      hibernate.format_sql: true

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG